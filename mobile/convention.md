## Conventions TypeScript
- Même règles que le back — Do's & Don'ts officiels
- JSX components : retour `ReactElement` explicite
- `interface` pour les props, `type` pour les unions
- Jamais `any`, `catch (err: unknown)`
- Imports alias : `@/` → `src/`
- Chaque nouveau composant doit être créer dans un fichier à part
- Sépare le css du html
- Privilégie la creation de nouvelle classes css à l'utilisation des balises style