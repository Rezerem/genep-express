import React, { ReactElement } from 'react'
import { Map } from './MapComponent'

/**
 * Écran principal de la carte
 * Point d'entrée pour la route /client/map
 */
export default function MapScreen(): ReactElement {
  return <Map />
}
